#include <stdio.h>

int main()
{
    int idades[5];
    int soma = 0;
    
    for(int i = 0; i < 5; i++){
        printf("Digite a idade do aluno %d: ", i+1);
        scanf("%d", &idades[i]);
        soma += idades[i];
    }
    
    printf("A soma das idades: %d\n", soma);
    
    return 0;
}
