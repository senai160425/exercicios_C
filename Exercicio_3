//Preencher um vetor com 6 números e mostrá-los na tela.

#include <stdio.h>
#include <stdio.h>

int main() {
    int vetor[5];

    for (int i = 0; i < 5; i++) {
        scanf("%d", &vetor[i]);
    }
    for (int i = 0; i < 5; i++) {
        printf("%d ", vetor[i]);
    }

    return 0;
}
